RSpec.describe Nginx::Unit::Model do
  it "has a version number" do
    expect(Nginx::Unit::Model::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
